First we have two space-separated integers denoting the respective values of n(the number of integers) and d(the number of left rotations you must perform). Then we have n space-separated integers describing the respective elements of the array's initial state.